# Models

Trained model weights are stored here. These files are git-ignored due to size.

## Expected models:
- `action_classifier.pt` — Pose-sequence action classifier
- `fighter_detector.pt` — Fighter bounding box detector (or use YOLO pretrained)
